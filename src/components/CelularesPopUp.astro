---

---


<div class="popup-overlay" id="popup">
  <div class="popup-card">
    <button class="close-button" onclick="closePopup()"><span>✖</span></button>
    <h2>¿Tu celular o laptop necesita ayuda?</h2>
    <p>Ofrecemos reparación profesional para dispositivos móviles. ¡Diagnóstico gratuito!</p>
    <button class="cta-button">Solicita tu reparación</button>
  </div>
</div>


<style>
  body {
    margin: 0;
    font-family: Arial, sans-serif;
    background-color: #f5f5f5;
  }

  /* Fondo oscuro tras la card */
  .popup-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    background-color: rgba(0, 0, 0, 0.5);
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 1000;
  }

  /* Card del anuncio */
  .popup-card {
    background-color: #fff;
    border-radius: 16px;
    padding: 24px;
    width: 90%;
    max-width: 400px;
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
    position: relative;
    animation: fadeInUp 0.4s ease;
    text-align: center;
  }

  .popup-card h2 {
    margin-top: 0;
    color: #333;
  }

  .popup-card p {
    color: #555;
    margin: 12px 0;
  }

  .popup-card .cta-button {
    background-color: #2e86de;
    color: white;
    border: none;
    padding: 12px 20px;
    border-radius: 8px;
    cursor: pointer;
    font-size: 1rem;
    margin-top: 16px;
    transition: background-color 0.3s;
  }

  .popup-card .cta-button:hover {
    background-color: #1b4f72;
  }

  .close-button {
    position: absolute;
    top: 12px;
    right: 12px;
    background: transparent;
    border: none;
    font-size: 1.2rem;
    cursor: pointer;
    color: #999;
  }

  .close-button:hover {
    color: #333;
  }

  @keyframes fadeInUp {
    from {
      transform: translateY(40px);
      opacity: 0;
    }
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }
</style>

<script>
  function closePopup() {
    const popup = document.getElementById('popup');
    if (popup) {
      popup.style.display = 'none';
    }
  }
</script>